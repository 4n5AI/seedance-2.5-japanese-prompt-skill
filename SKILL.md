---
name: seedance-25
description: "Seedance 2.5（シーダンス2.5／Seedance2.5／SD2.5）の動画プロンプトを作成・修正・翻訳・短縮するときに必ず毎回使う。会話の続き、リップシンク用セリフ、参照素材、時間指定、長尺・延長・部分編集のプロンプトも対象。DreaminaとByteDanceの公式資料に加え、カメラワーク・ショットサイズ・レンズと焦点距離・構図・ライティングのシネマトグラフィー知識を基に構成し、日本語セリフの難しい漢字はひらがな、数字は算用数字、英語はカタカナに整える。明示されたSeedance 2.0や他モデルの依頼には自動適用しない。Use this skill for every Seedance 2.5 video prompt request in Japanese (create, revise, translate, shorten, lip-sync dialogue, camera and lighting direction); not for Seedance 2.0 or other models."
metadata:
  source_reviewed: "2026-09-08"
---

# Seedance 2.5 プロンプト

ユーザーの映像意図を、貼り付けて使えるSeedance 2.5用プロンプトにする。Seedance 2.5のプロンプトを依頼されたら、追加指示や再生成用の修正でも、このSkillを毎回適用する。同じ会話で読んだ本文が有効なら再読は不要だが、以下の表記確認は毎回行う。

## ユーザー指定：リップシンクのセリフ表記

発話する日本語セリフには必ず次の3規則を適用する。これらはWeb資料の一般仕様ではなく、ユーザー本人の希望である。

1. **AIが読みにくい難しい漢字は、ひらがなにする。** 難読語、専門語、読みが紛らわしい語を整える。例：叡智→えいち、躊躇→ちゅうちょ、憧憬→しょうけい。意味や語を変えず読みへ変換する。普通に読める漢字まで一律にひらがなにしない。固有名詞の読みが確定できないときだけ確認する。
2. **数値は算用数字で表す。** 二千年→2,000年、一九二回／百九十二回→192回、三万円→30,000円。年・回・円などの単位、数値、桁、符号、小数を保つ。発音を助ける目的でも数値を漢数字やひらがなに戻さない。「一緒」「唯一」のような数値でない語は機械的に数字へ変換しない。
3. **英語は発音に合うカタカナで表す。** AI→エーアイ、YouTube→ユーチューブ、Hello→ハロー。英字の略語・商品名も、実際に話す部分はカタカナにする。意味を勝手に翻訳しない。

引用内のセリフと、別掲するセリフ一覧の両方を整え、同じ文にする。表記の指定は発話部分に限定する。`@Image1`などの参照タグ、ファイル名、URL、API識別子、話さない画面上の文字は改変しない。字幕表記を求められた場合は発話テキストと分ける。

原文「二千年の叡智をAIで未来へ。百九十二回の挑戦。」なら、発話文は「2,000年のえいちをエーアイで未来へ。192回の挑戦。」とする。この例は表記の説明であり、全動画へ挿入する定型文ではない。

## 作成手順

- 既存の会話から用途、主役、尺、縦横比、素材、音声、利用サービスを引き継ぐ。未指定なら合理的な仮定を短く示して作成する。2.5の文脈で「続き」「短くして」も対象。明示された2.0の依頼は2.0として扱う。
- 下記の組み立てを使い、短い単一ショットなら数文、複数ショットなら時間帯ごとに書く。ユーザーが指定した尺を30秒へ勝手に変更しない。
- 素材は実際のアップロード順・表示タグに対応させ、顔、商品、動き、カメラ、声、背景など何を参照するかを定義する。**どの素材が何を制御するかを文中で明示する**（`@Image1 は主役の外見を定義する。@Video1 は動きを定義する。`）。役割を宣言せずに素材を並べない。未提供の素材を存在するものとして書かない。素材なしでも文章だけで成立するプロンプトを作る。
- 撮影を指示する際は下表の参照ファイルを読み、**映画用語（英語）＋日本語補足**のハイブリッド形式で記述する。ユーザーが「トラックアップ」「ズームアップ」などの日本の現場語や、`WS`・`BS` のような日本の略号を使った場合は、対応表で国際標準の英語へ変換してから書く。
- 数値設定・長尺・延長・編集・素材制限を扱う場合は [source-guide.md](references/source-guide.md) の該当項目を読む。料金、API項目、利用可能モードなど変わり得る事実を断定する前には、対象サービスの現行公式情報または実際の画面を確認する。確認できない環境では、未確認である旨を短く添える。プロンプト内の演出指定と、UI/APIで実際に選ぶ設定を分ける。

| 参照ファイル | 内容 |
|---|---|
| [prompt-syntax.md](references/prompt-syntax.md) | Seedance 2.5固有の記法。音声・字幕の括弧、時間指定の制約、参照素材の束縛、ネガティブ指定、モデルが解釈する語彙 |
| [camera-movements.md](references/camera-movements.md) | カメラの動き42種。日本の現場用語→英語の対応表 |
| [shot-composition.md](references/shot-composition.md) | ショットサイズ、アングルと高さ、構図。日本の略号の注意 |
| [lens-and-focus.md](references/lens-and-focus.md) | 焦点距離と画角、被写界深度、レンズのルック |
| [lighting-color.md](references/lighting-color.md) | 三点照明、光の質、時間帯、色温度、カラーグレード |
| [multi-shot-continuity.md](references/multi-shot-continuity.md) | 複数ショットの連続性、編集文法、絵コンテとの対応 |
| [source-guide.md](references/source-guide.md) | 出典、資料上の数値条件、モード別の書き方 |

**Seedance 2.0向けの助言を2.5に適用しない。** 特に時間指定は方針が逆で、2.0は「タイムスタンプを付けない」、2.5は秒単位の区間指定に対応する。

## プロンプトの組み立て

基本の骨格は **素材の役割 → 映像の要約 → 時間順の展開 → 全体の補足**。必要な情報は以下の8項目で確認する。毎回すべてを見出しにする必要はない。

| 項目 | 記述する内容 |
|---|---|
| 形式 | 尺、縦横比、連続撮影（`one-take shot`）か複数ショットか |
| 主役 | 人物・商品の識別特徴、維持する衣装や形状 |
| 動作 | 始まりと終わりが見える具体的な動き |
| 環境 | 場所、時刻、天候、空間関係 |
| カメラ | ショットサイズ・アングルと高さ・主な移動・レンズと被写界深度（[shot-composition.md](references/shot-composition.md)、[camera-movements.md](references/camera-movements.md)、[lens-and-focus.md](references/lens-and-focus.md)） |
| 見た目 | 光源と光の質、時間帯、色調・グレード、材質、写実／アニメなど（[lighting-color.md](references/lighting-color.md)） |
| 時間 | 各動作・ショットの区間と切り替え |
| 音・制約 | 発話者、セリフ（3規則適用）、環境音・効果音・音楽、維持／除外する要素 |

### カメラ設計の5原則
1. **映画用語（英語）＋日本語補足**: `Slow dolly in`、`Orbit 180`、`FPV drone dive`、`Rack focus` などの英語キーワードを明記し、続けて日本語で対象と意図を補足する。プロンプト全体は日本語で書いてよい。
2. **和製英語・日本の略号を変換する**: 「トラックアップ」は `dolly in`、「ズームアップ」は `zoom in`、「パンアップ」は `tilt up`。`WS` は日本語のウエストショット（腰上）と英語の Wide Shot（引き）で意味が反転するため、略号を使わず綴る。「寄る」はレンズ操作（zoom）か本体移動（dolly）かを確定させる。
3. **1ショット1主要アクション**: 1つの時間区間に複数の急激なカメラ操作を重ねない（空間の歪み・被写体の崩れを防止）。速度語（`slowly` / `smoothly`）を必ず添える。
4. **相対関係の明示**: 被写体が動く場合、カメラが追従（Following/Tracking）か、静止（Static）か、並走（Side tracking）かを明記する。
5. **ルール名ではなく位置を書く**: 「三分割法で」「180度ルールを守って」だけでは伝わりにくい。`被写体は画面左1/3、視線の先の右側を空ける` のように、画面のどこに何をどの大きさで置くかを書く。

### 時間指定とショット構成

時間区間で書く場合は、**整数秒・隙間なし・1区間3秒以上**を守り、区間の合計を全体尺に一致させる。30秒なら区間は4〜6本までを目安にし、1区間＝1主要アクション＋1主要カメラ移動とする。秒を回数の指定（「1秒に3回うなずく」）に使わない。

時間を厳密に配分する必要がなく、テンポをモデルに委ねたいときは `Shot 1:` `Shot 2:` のラベル形式でもよい。ワンカットを狙うときは `one-take shot` と明記し、1つの進化する運動として書く。詳細と根拠は [prompt-syntax.md](references/prompt-syntax.md) を参照する。

「美しい」「映画的」だけに頼らず、画面で確認できる指示にする。各ショットのカメラ指示を両立させ、必要な制約だけを短く置く。複数人物は外見・立ち位置・発話者を区別する。ロゴや精密な文字の保持は保証せず、必要に応じて後編集を提案する。

## 音声と時間の確認

セリフは発話者と発話する時間帯を明示し、効果音も対応する動作へ結び付ける。声色の参照と、読み上げる文の参照を区別する。セリフが指定尺に収まるか、自然に読む速さと間を考慮する。発話量が多ければ短縮案を出し、数値表記はそのまま保つ。ユーザーが言っていないセリフや音楽の要否を全件共通の禁止事項にしない。

**言語・声質・話し方はセリフの外に先出しし、括弧の中は発話する語だけにする。** 例：`セリフ言語：日本語。落ち着いた低めの声。{2,000年のえいちをエーアイで未来へ。}`

音楽・環境音は `( )`、効果音は `< >`、発話は `{ }`、画面上の字幕は `【 】` で書き分けられる。**セリフと字幕は別チャンネルなので、聞かせたい かつ 見せたい場合は両方に同じ文を書く。** 括弧は任意で、平文でも動作する。リップシンクの3規則は**発話するテキスト**に適用する。なお `【 】` は画面に描画される文字の指定なので、プロンプトの見出し・ラベルには使わない（見出しは `■` などにする）。

括弧の割り当てと時間指定の制約は、複数の第三者解説が一致して述べるもので**公式の一次文面は未確認**なので、ユーザーへ断定して伝えない。詳細と確度は [prompt-syntax.md](references/prompt-syntax.md) を参照する。

除外したい要素があるときは `no subtitles`・`no background music`・`no jump cuts` のように平文で書き、該当区間と末尾の2か所に置くと効きやすい。ユーザーが求めていない禁止事項を足さない。

提出前に以下を確認する。

- 時間区間が整数秒で隙間なく連続し、合計が全体尺と一致している。1区間が3秒以上で、区間数が過剰でない。
- ワンカット指定と途中のカット、固定撮影と移動指示が衝突していない。
- カメラ指示が各区間で1つ（多くて2つ）に絞られ、速度語が添えられ、物理的に成立している。
- 日本の現場用語・略号が国際標準の英語へ変換されており、「寄る」がレンズ操作か本体移動か確定している。
- 複数ショットの場合、隣接ショットのサイズと角度が変わっており、人物の識別特徴・画面上の左右の立ち位置・光源の方向が各ショットに明記されている。
- 参照素材・人物・発話者の対応が一貫し、各素材の役割が宣言され、架空の素材タグがない。
- 全セリフの難読漢字、漢数字の数値、英語の残存を文脈ごとに点検した（ひらがな・算用数字・カタカナ）。

## 出力

まず完成プロンプトを提示する。通常はコピーしやすいコードブロックに収め、設定や仮定が必要なときだけ短く添える。ユーザーがセリフだけを求めた場合は整えたセリフだけを返す。プロンプト作成だけの依頼から、実生成・アップロード・クレジット消費へ作業を広げない。

### プロンプト構成例（時間指定・ワンカット）
```text
■形式：10秒、16:9、実写映画調、one-take shot（連続したカメラワーク、カット割りなし）
■主役：30代の日本人女性研究者。白衣、黒縁の眼鏡、後ろでまとめた黒髪。全区間で同一人物を維持する。
■環境：夜の静かな先端研究所。背景の大型モニターに青いデータグラフが発光している。
■見た目：practical light（モニターの青い実光源）が顔を照らし、机上のデスクランプが暖色のリムライトで髪を縁取る。
　　　　　短めの望遠による浅い被写界深度（shallow depth of field）、寒色に寄せた階調に暖色のハイライト。
■構図：被写体は画面左1/3、視線の先の右側を空ける（rule of thirds、lead room）。

■タイムライン演出
0〜3秒:
- カメラ：Medium shot、eye level、static shot。カメラは固定のまま、デスクで顕微鏡を覗き込む女性を捉える。
- 動作：女性が真剣な表情でレンズから目を離し、手元のノートへペンを走らせる。
- 音声：(静かな空調音のアンビエント) <ペンが紙を走る音>

3〜7秒:
- カメラ：Slow dolly in。カメラ自体が正面へゆっくり前進し、medium shot から medium close-up へ寄る。
- 動作：女性が顔を上げ、正面のモニターを見つめながら微笑む。
- 発話（女性）：セリフ言語：日本語。落ち着いた低めの声、ゆっくりと。
　{2,000時間のけんさんが、ついに実を結びました。}（原文「2,000時間の研鑽が…」。研鑽は難読語なのでひらがなにし、数値は算用数字のまま保つ）

7〜10秒:
- カメラ：Rack focus。女性から背後のモニター画面へピントがゆっくり移動し、データグラフが鮮明になる。
- 動作：モニター上のプログレスバーが100%に達し、緑色に点灯する。
- 音声：<控えめな電子決定音> (奥深いアンビエントBGM)

■全体の維持・除外：全区間で人物の外見と光源の方向を維持する。no jump cuts、no subtitles。
```

この例は書式の説明であり、依頼ごとに新しく書く。括弧記法は任意なので、ユーザーが平文を望む場合は使わない。

根拠とモード別条件は [source-guide.md](references/source-guide.md)、Seedance 2.5固有の記法は [prompt-syntax.md](references/prompt-syntax.md)、撮影の語彙は [camera-movements.md](references/camera-movements.md)・[shot-composition.md](references/shot-composition.md)・[lens-and-focus.md](references/lens-and-focus.md)・[lighting-color.md](references/lighting-color.md)・[multi-shot-continuity.md](references/multi-shot-continuity.md) に保存してある。元ページの長文例・広告文の転載はせず、必要な指針を使って依頼ごとに新しく書く。
